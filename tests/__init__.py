"""Test suite for PyRecycle-Analytics.

The package is importable (rather than a bare directory of scripts) because
``tests.synthetic_data`` is a first-class module: it is the ground-truth generator
every chemometric test scores against, and later milestones import it directly.
"""
